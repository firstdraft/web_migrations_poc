<%= form_with(model: web_table, local: true) do |form| %>
  <% if web_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(web_table.errors.count, "error") %> prohibited this web_table from being saved:</h2>

      <ul>
      <% web_table.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
